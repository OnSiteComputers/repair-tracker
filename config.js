/* =====================================================================
   CONFIG — Fill in these two values after creating your Supabase project.
   See SETUP-GUIDE for exactly where to find them (Project Settings → API).
   ===================================================================== */

window.REPAIR_TRACKER_CONFIG = {
  // 1) Your Supabase Project URL (looks like: https://abcdefgh.supabase.co)
  SUPABASE_URL: "https://pnghrbplmkspvhkdgpou.supabase.co",

  // 2) Your Supabase "anon public" API key (a long string starting with eyJ...)
  SUPABASE_ANON_KEY: "sb_publishable_0SQO7uLgTRCnx9jA5CmulQ_u9GUelLl",

  // 3) The password YOU use to log in to the management view.
  //    (Linda's status page needs no password — it's read-only.)
  //    Change this to whatever you want. It is NOT a security wall by itself —
  //    the real protection is the database rules in the setup SQL — but it keeps
  //    the management screen out of casual view.
  MANAGER_PASSWORD: "Graysonis#1!",
};
